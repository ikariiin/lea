{
  "name": "lea",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "copy-dist": "cp -r packages/frontend/dist ./dist",
    "clean": "rm -rf dist"
  }
}
